<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= quest.name %> - Loyalty Quests</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: #f5f5f7;
      color: #333;
      line-height: 1.6;
    }

    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 24px 20px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .header-content {
      max-width: 900px;
      margin: 0 auto;
    }

    .back-link {
      display: inline-flex;
      align-items: center;
      color: white;
      text-decoration: none;
      font-size: 14px;
      margin-bottom: 16px;
      opacity: 0.9;
      transition: opacity 0.2s;
    }

    .back-link:hover {
      opacity: 1;
    }

    .header h1 {
      font-size: 32px;
      margin-bottom: 8px;
    }

    .container {
      max-width: 900px;
      margin: 0 auto;
      padding: 32px 20px;
    }

    .quest-status {
      display: inline-block;
      padding: 6px 16px;
      border-radius: 20px;
      font-size: 13px;
      font-weight: 600;
      margin-bottom: 16px;
    }

    .quest-status.not-started {
      background: #e3e3e3;
      color: #666;
    }

    .quest-status.in-progress {
      background: #fff3cd;
      color: #856404;
    }

    .quest-status.completed {
      background: #d4edda;
      color: #155724;
    }

    .card {
      background: white;
      border-radius: 12px;
      padding: 32px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
      margin-bottom: 24px;
    }

    .description {
      font-size: 16px;
      color: #666;
      margin-bottom: 32px;
      line-height: 1.8;
    }

    .progress-section {
      margin-bottom: 32px;
    }

    .progress-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 16px;
    }

    .progress-label {
      font-size: 18px;
      font-weight: 600;
    }

    .progress-percent {
      font-size: 28px;
      font-weight: 700;
      color: #667eea;
    }

    .progress-bar {
      background: #e9ecef;
      border-radius: 12px;
      height: 24px;
      margin-bottom: 12px;
      overflow: hidden;
      position: relative;
    }

    .progress-fill {
      background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
      height: 100%;
      border-radius: 12px;
      transition: width 0.5s ease;
      display: flex;
      align-items: center;
      justify-content: flex-end;
      padding-right: 12px;
    }

    .progress-fill-text {
      color: white;
      font-size: 13px;
      font-weight: 600;
    }

    .progress-details {
      display: flex;
      justify-content: space-between;
      font-size: 14px;
      color: #666;
    }

    .motivational-text {
      background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
      border-left: 4px solid #667eea;
      padding: 16px 20px;
      border-radius: 8px;
      font-size: 15px;
      color: #555;
      margin-top: 20px;
    }

    .section-title {
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 16px;
      display: flex;
      align-items: center;
    }

    .conditions-list {
      list-style: none;
    }

    .condition-item {
      background: #f8f9fa;
      padding: 16px;
      border-radius: 8px;
      margin-bottom: 12px;
      display: flex;
      align-items: center;
    }

    .condition-icon {
      width: 40px;
      height: 40px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 16px;
      font-size: 20px;
      flex-shrink: 0;
    }

    .condition-text {
      flex: 1;
      font-size: 15px;
      color: #555;
    }

    .rewards-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 16px;
    }

    .reward-item {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 24px;
      border-radius: 12px;
      text-align: center;
    }

    .reward-icon {
      font-size: 40px;
      margin-bottom: 12px;
    }

    .reward-type {
      font-size: 14px;
      opacity: 0.9;
      margin-bottom: 8px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .reward-value {
      font-size: 24px;
      font-weight: 700;
      margin-bottom: 4px;
    }

    .reward-description {
      font-size: 13px;
      opacity: 0.85;
    }

    .dates-info {
      display: flex;
      gap: 24px;
      padding: 20px;
      background: #f8f9fa;
      border-radius: 8px;
      font-size: 14px;
    }

    .date-item {
      flex: 1;
    }

    .date-label {
      color: #666;
      margin-bottom: 4px;
      font-size: 13px;
    }

    .date-value {
      font-weight: 600;
      color: #333;
    }

    @media (max-width: 768px) {
      .header h1 {
        font-size: 24px;
      }

      .card {
        padding: 24px 20px;
      }

      .rewards-grid {
        grid-template-columns: 1fr;
      }

      .dates-info {
        flex-direction: column;
        gap: 12px;
      }
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="header-content">
      <a href="/apps/quests" class="back-link">← Back to All Quests</a>
      <h1><%= quest.name %></h1>
    </div>
  </div>

  <div class="container">
    <div class="card">
      <span class="quest-status <%= quest.progress.status.toLowerCase().replace('_', '-') %>">
        <%= quest.progress.status.replace('_', ' ') %>
      </span>

      <p class="description"><%= quest.description %></p>

      <!-- Progress Section -->
      <div class="progress-section">
        <div class="progress-header">
          <span class="progress-label">Your Progress</span>
          <span class="progress-percent"><%= quest.progress.percentComplete %>%</span>
        </div>

        <div class="progress-bar">
          <div class="progress-fill" style="width: <%= quest.progress.percentComplete %>%">
            <% if (quest.progress.percentComplete > 20) { %>
              <span class="progress-fill-text"><%= quest.progress.currentValue %> / <%= quest.progress.targetValue %></span>
            <% } %>
          </div>
        </div>

        <div class="progress-details">
          <span>Current: <%= quest.progress.currentValue %></span>
          <span>Target: <%= quest.progress.targetValue %></span>
        </div>

        <% if (quest.progress.status === 'IN_PROGRESS') { %>
          <% const remaining = quest.progress.targetValue - quest.progress.currentValue; %>
          <div class="motivational-text">
            💪 Only <%= remaining %> more to go! Keep going to unlock your reward!
          </div>
        <% } else if (quest.progress.status === 'COMPLETED') { %>
          <div class="motivational-text">
            🎉 Congratulations! You've completed this quest! Check your rewards below.
          </div>
        <% } else { %>
          <div class="motivational-text">
            🚀 Ready to start? Begin your journey to earn amazing rewards!
          </div>
        <% } %>
      </div>

      <!-- Quest Dates -->
      <% if (quest.startDate || quest.endDate) { %>
        <div class="dates-info">
          <% if (quest.startDate) { %>
            <div class="date-item">
              <div class="date-label">Started</div>
              <div class="date-value"><%= new Date(quest.startDate).toLocaleDateString() %></div>
            </div>
          <% } %>
          <% if (quest.endDate) { %>
            <div class="date-item">
              <div class="date-label">Ends</div>
              <div class="date-value"><%= new Date(quest.endDate).toLocaleDateString() %></div>
            </div>
          <% } %>
        </div>
      <% } %>
    </div>

    <!-- Conditions -->
    <% if (quest.conditions && quest.conditions.length > 0) { %>
      <div class="card">
        <h2 class="section-title">📋 Quest Requirements</h2>

        <ul class="conditions-list">
          <% quest.conditions.forEach(condition => { %>
            <li class="condition-item">
              <div class="condition-icon">
                <% if (condition.type === 'ORDER_COUNT') { %>🛍️<% } %>
                <% if (condition.type === 'ORDER_VALUE') { %>💰<% } %>
                <% if (condition.type === 'PRODUCT_PURCHASE') { %>📦<% } %>
                <% if (condition.type === 'COLLECTION_PURCHASE') { %>🏷️<% } %>
              </div>
              <div class="condition-text">
                <% if (condition.type === 'ORDER_COUNT') { %>
                  Make <%= condition.config.requiredCount %> purchase<%= condition.config.requiredCount > 1 ? 's' : '' %>
                <% } else if (condition.type === 'ORDER_VALUE') { %>
                  Spend $<%= condition.config.requiredValue %>
                <% } else if (condition.type === 'PRODUCT_PURCHASE') { %>
                  Purchase a specific product
                <% } else { %>
                  <%= condition.type.replace('_', ' ') %>
                <% } %>
              </div>
            </li>
          <% }); %>
        </ul>
      </div>
    <% } %>

    <!-- Rewards -->
    <% if (quest.rewards && quest.rewards.length > 0) { %>
      <div class="card">
        <h2 class="section-title">🎁 Rewards</h2>

        <div class="rewards-grid">
          <% quest.rewards.forEach(reward => { %>
            <div class="reward-item">
              <div class="reward-icon">
                <% if (reward.type === 'DISCOUNT_CODE') { %>🎟️<% } %>
                <% if (reward.type === 'FREE_SHIPPING') { %>🚚<% } %>
                <% if (reward.type === 'FREE_PRODUCT') { %>🎁<% } %>
                <% if (reward.type === 'STORE_CREDIT') { %>💳<% } %>
              </div>
              <div class="reward-type"><%= reward.type.replace('_', ' ') %></div>
              <div class="reward-value">
                <% if (reward.config.discountType === 'PERCENTAGE') { %>
                  <%= reward.config.discountValue %>% OFF
                <% } else if (reward.config.discountType === 'FIXED_AMOUNT') { %>
                  $<%= reward.config.discountValue %> OFF
                <% } else { %>
                  Exclusive Reward
                <% } %>
              </div>
              <% if (reward.config.minimumPurchase) { %>
                <div class="reward-description">
                  Min. purchase: $<%= reward.config.minimumPurchase %>
                </div>
              <% } %>
            </div>
          <% }); %>
        </div>
      </div>
    <% } %>
  </div>
</body>
</html>
