<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Quests - Loyalty Quests</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: #f5f5f7;
      color: #333;
      line-height: 1.6;
    }

    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 24px 20px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .header-content {
      max-width: 1200px;
      margin: 0 auto;
    }

    .header h1 {
      font-size: 28px;
      margin-bottom: 8px;
    }

    .header p {
      font-size: 14px;
      opacity: 0.9;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 24px 20px;
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 16px;
      margin-bottom: 32px;
    }

    .stat-card {
      background: white;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }

    .stat-value {
      font-size: 32px;
      font-weight: 700;
      color: #667eea;
      margin-bottom: 4px;
    }

    .stat-label {
      font-size: 14px;
      color: #666;
    }

    .section {
      margin-bottom: 40px;
    }

    .section-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 16px;
    }

    .section-title {
      font-size: 22px;
      font-weight: 600;
    }

    .quests-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 20px;
    }

    .quest-card {
      background: white;
      border-radius: 12px;
      padding: 24px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
      transition: transform 0.2s, box-shadow 0.2s;
      cursor: pointer;
      text-decoration: none;
      color: inherit;
      display: block;
    }

    .quest-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
    }

    .quest-status {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
      margin-bottom: 12px;
    }

    .quest-status.not-started {
      background: #e3e3e3;
      color: #666;
    }

    .quest-status.in-progress {
      background: #fff3cd;
      color: #856404;
    }

    .quest-status.completed {
      background: #d4edda;
      color: #155724;
    }

    .quest-title {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .quest-description {
      font-size: 14px;
      color: #666;
      margin-bottom: 16px;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }

    .progress-bar {
      background: #e9ecef;
      border-radius: 10px;
      height: 8px;
      margin-bottom: 8px;
      overflow: hidden;
    }

    .progress-fill {
      background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
      height: 100%;
      border-radius: 10px;
      transition: width 0.3s ease;
    }

    .progress-text {
      font-size: 13px;
      color: #666;
      display: flex;
      justify-content: space-between;
    }

    .reward-badge {
      display: inline-flex;
      align-items: center;
      background: #f0f4ff;
      color: #667eea;
      padding: 6px 12px;
      border-radius: 6px;
      font-size: 13px;
      margin-top: 12px;
      font-weight: 500;
    }

    .rewards-list {
      display: grid;
      gap: 16px;
    }

    .reward-card {
      background: white;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .reward-info {
      flex: 1;
    }

    .reward-title {
      font-size: 16px;
      font-weight: 600;
      margin-bottom: 4px;
    }

    .reward-meta {
      font-size: 13px;
      color: #666;
    }

    .reward-code {
      background: #f8f9fa;
      border: 2px dashed #dee2e6;
      border-radius: 8px;
      padding: 12px 16px;
      font-family: 'Courier New', monospace;
      font-size: 16px;
      font-weight: 700;
      color: #667eea;
      margin-left: 16px;
      white-space: nowrap;
    }

    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: #999;
    }

    .empty-state-icon {
      font-size: 64px;
      margin-bottom: 16px;
    }

    .empty-state-text {
      font-size: 16px;
    }

    @media (max-width: 768px) {
      .quests-grid {
        grid-template-columns: 1fr;
      }

      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .reward-card {
        flex-direction: column;
        align-items: flex-start;
      }

      .reward-code {
        margin-left: 0;
        margin-top: 12px;
      }
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="header-content">
      <h1>🎮 My Quests</h1>
      <p>Complete quests and earn exclusive rewards</p>
    </div>
  </div>

  <div class="container">
    <!-- Stats Overview -->
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-value"><%= stats.totalQuests %></div>
        <div class="stat-label">Total Quests</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= stats.completedQuests %></div>
        <div class="stat-label">Completed</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= stats.inProgressQuests %></div>
        <div class="stat-label">In Progress</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= stats.totalRewards %></div>
        <div class="stat-label">Rewards Earned</div>
      </div>
    </div>

    <!-- Active Quests -->
    <div class="section">
      <div class="section-header">
        <h2 class="section-title">Active Quests</h2>
      </div>

      <% if (quests.length === 0) { %>
        <div class="empty-state">
          <div class="empty-state-icon">📭</div>
          <div class="empty-state-text">No active quests available right now. Check back soon!</div>
        </div>
      <% } else { %>
        <div class="quests-grid">
          <% quests.forEach(quest => { %>
            <a href="/apps/quests/<%= quest.id %>" class="quest-card">
              <span class="quest-status <%= quest.progress.status.toLowerCase().replace('_', '-') %>">
                <%= quest.progress.status.replace('_', ' ') %>
              </span>

              <h3 class="quest-title"><%= quest.name %></h3>
              <p class="quest-description"><%= quest.description %></p>

              <div class="progress-bar">
                <div class="progress-fill" style="width: <%= quest.progress.percentComplete %>%"></div>
              </div>

              <div class="progress-text">
                <span><%= quest.progress.currentValue %> / <%= quest.progress.targetValue %></span>
                <span><%= quest.progress.percentComplete %>%</span>
              </div>

              <% if (quest.rewards && quest.rewards.length > 0) { %>
                <div class="reward-badge">
                  🎁 <%= quest.rewards.length %> reward<%= quest.rewards.length > 1 ? 's' : '' %> available
                </div>
              <% } %>
            </a>
          <% }); %>
        </div>
      <% } %>
    </div>

    <!-- Available Rewards -->
    <% if (rewards.length > 0) { %>
      <div class="section">
        <div class="section-header">
          <h2 class="section-title">🎁 Available Rewards</h2>
        </div>

        <div class="rewards-list">
          <% rewards.forEach(reward => { %>
            <div class="reward-card">
              <div class="reward-info">
                <div class="reward-title"><%= reward.questName %></div>
                <div class="reward-meta">
                  Earned <%= new Date(reward.issuedAt).toLocaleDateString() %>
                  <% if (reward.expiresAt) { %>
                    • Expires <%= new Date(reward.expiresAt).toLocaleDateString() %>
                  <% } %>
                </div>
              </div>
              <% if (reward.discountCode) { %>
                <div class="reward-code"><%= reward.discountCode %></div>
              <% } %>
            </div>
          <% }); %>
        </div>
      </div>
    <% } %>

    <!-- Reward History -->
    <% if (rewardsHistory.length > 0) { %>
      <div class="section">
        <div class="section-header">
          <h2 class="section-title">📜 Reward History</h2>
        </div>

        <div class="rewards-list">
          <% rewardsHistory.forEach(reward => { %>
            <div class="reward-card" style="opacity: 0.6;">
              <div class="reward-info">
                <div class="reward-title"><%= reward.questName %></div>
                <div class="reward-meta">
                  <% if (reward.isRedeemed) { %>
                    ✅ Redeemed <%= new Date(reward.redeemedAt).toLocaleDateString() %>
                  <% } else if (reward.isExpired) { %>
                    ⏰ Expired <%= new Date(reward.expiresAt).toLocaleDateString() %>
                  <% } %>
                </div>
              </div>
              <% if (reward.discountCode) { %>
                <div class="reward-code" style="opacity: 0.5;"><%= reward.discountCode %></div>
              <% } %>
            </div>
          <% }); %>
        </div>
      </div>
    <% } %>
  </div>
</body>
</html>
